using VContainer;
using VContainer.Unity;

namespace Spellcast.Infrastructure
{
    public class ContainerRegistrations : IInstaller
    {
        public void Install(IContainerBuilder builder)
        {
            
        }
    }
}
